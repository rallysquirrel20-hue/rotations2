# Session Log — 2026-03-25

## 14:00 — Live Signals Debugging

**Type**: Investigation / Bug Fix
**Files modified**: None (deleted corrupted cache file)
**What was done**: Started PM2 ecosystem config for rotations. Live-signals process was crash-looping. Investigated logs — found two issues: (1) NDU not initially running (resolved by user starting it), (2) corrupted ETF signals cache file `signals_etf_50.parquet` causing `OSError: Unexpected end of stream`. Deleted the corrupted 112MB file so pipeline can rebuild on next run.
**Result**: Success
**Details**:
- `pd.read_parquet(ETF_SIGNALS_CACHE_FILE)` at rotations.py:3154 hit truncated parquet
- File was written 2026-03-24 at 14:46, apparently truncated during a write
- After deleting, confirmed norgatedata connection works (tested `security_name('AAPL')`)
- Restarted live-signals via PM2

**Follow-up needed**: Verify ETF signals cache rebuilds cleanly on next full run

---

## 14:30 — Incremental Basket Rebuild Optimization (Feature Branch)

**Type**: Change
**Branch**: `optimize-incremental-rebuild`
**Files modified**:
- `signals/rotations.py` — Added 3 new functions + wiring (~200 lines added)
  - `_compute_within_basket_correlation_incremental` (after line ~4484)
  - `_compute_and_save_contributions_incremental` (after line ~4810)
  - `_finalize_basket_signals_output` — added `incremental_dates` parameter
  - Incremental append path (line ~5199) — passes `_new_dates` to finalize
- `signals/test_incremental_benchmark.py` (new, ~590 lines)
- `signals/prep_test_data.py` (new, ~115 lines)
- `signals/test_incremental_run.py` (new, ~530 lines)
- `.claude/optimize_basket_rebuild.md` (new, full plan document)

**What was done**: Identified that the "incremental" basket rebuild path was recomputing correlation, contributions, and breadth pivots over FULL history for all 27 baskets even when only 1-2 new days were appended. Implemented truly incremental versions of correlation and contributions. Created test harness, benchmark script, and test data for user verification.

**Result**: Success (pending merge)

**Details**:
- Correlation: 171x speedup (1.2s → 0.007s per basket), exact match with production
- Contributions: 6.6x speedup (0.6s → 0.09s per basket), exact match with production
- Fixed multi-day weight chaining bug (contributions BOD weights must chain across consecutive new dates via `_carried_bod`)
- All 30 baskets validated against production — ALL MATCH (max diff ~2e-14, floating point noise)
- Projected 27-basket correlation savings: 33s → 0.2s
- Full 30-basket test run: 5.8s total for correlation + contributions (vs ~45s full recompute)
- Test data created: all basket files with 2026-03-24 stripped in `signals/test_data/`
- Breadth pivots left as full recompute (Phase 2, ~0.1s/basket, not urgent)

**Follow-up needed**:
- User to run `test_incremental_run.py` to verify before merge
- After merge to main: run dependency-mapper and integration-tracker agents
- Phase 2 (breadth pivot state caching) deferred pending Phase 1 in-production results
- Phase 3 (basket-level parallelism) deferred — likely unnecessary given Phase 1 speedups
