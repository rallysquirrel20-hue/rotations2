# Session Log — 2026-04-02

## Pipeline Deployment & Data Transition

**Type**: Infrastructure overhaul + backend/frontend fixes
**Trigger**: New multi-file signal pipeline ready for production deployment

---

### Data Transition

- Ran `build_universes.py`, `build_signals.py`, `build_baskets.py` with `DATA_STORAGE_NAME=data_storage_test` to validate outputs
- Installed missing `exchange_calendars` dependency (`python -m pip install exchange_calendars`)
- Copied `data_storage_test/` over `Data_Storage/` — new data includes delisted securities filtering via quarterly universe keying

### Live Signals Overhaul (`signals/live_updates.py`)

- **Rewrote `export_today_signals()`** — removed Excel export entirely, removed duplicate OHLC-only parquet writes
- Now writes full signal rows (all columns matching `signals_500.parquet`) to `live_signals_500.parquet` with `Source='live'`
- Each ticker gets a complete `_build_signals_next_row()` result, not just OHLC
- Removed `signal_flags` pivot logic and `_sort_signals_df()` usage — output is one row per ticker sorted by Ticker

### PM2 Process Architecture

**Killed**: `live-signals` (ran `rotations.py` every 15 min via `live_loop.py`)

**Created 3 new loop scripts** using `exchange_calendars` NYSE schedule:

| Process | Script | Schedule |
|---|---|---|
| `rot-universes` | `loop_universes.py` → `build_universes.py` | 5pm ET, last trading day of quarter |
| `rot-signals` | `loop_signals.py` → `build_signals.py` + `build_baskets.py` | 5pm ET, all 251 trading days |
| `rot-live` | `loop_live.py` → `live_updates.py` | Every 5 min, 9:30–4:00 ET, trading days |

- All use pre-computed NYSE trading calendar; auto-refresh on Jan 1
- Updated `ecosystem.config.js` with process definitions and documentation comments
- pm2 state saved for persistence across reboots

### Backend Fixes (`app/backend/main.py`)

1. **Summary endpoint crash** — `GET /api/baskets/{name}/summary` requested rolling stats columns (Win_Rate, EV, etc.) missing from new parquet. Added `pyarrow.parquet.read_schema()` to filter to available columns only.

2. **THEMATIC_CONFIG key mismatch** — `dividend_universes_500.json` uses keys `'growth'` and `'with_growth'`, not `'div_growth'`/`'div_with_growth'`. Was crashing analogs endpoint with `KeyError: 'div_growth'`.

3. **Correlation in sidebar** — Breadth endpoint replaced per-basket `_tally_breadth()` calls with single `_compute_live_breadth_batch()` call which computes Uptrend_Pct, Breakout_Pct, AND Correlation_Pct. Added fallback to most recent non-NaN `Correlation_Pct` when last basket row is NaN.

4. **Industry size filter** — Added `MIN_INDUSTRY_TICKERS = 3` constant and `_is_valid_basket()` helper. Filters industries with < 3 current-quarter tickers from:
   - `/api/baskets` (sidebar list)
   - `/api/baskets/breadth` (sidebar metrics)
   - `/api/baskets/compositions` (quarter compositions)
   - `/api/baskets/returns` (cross-basket, analogs)
   - `/api/signals/log` (basket signal log)
   - Result: 117 → 69 industries displayed

### Frontend Cleanup

- **Removed signal tabs** from `BasketSummary.tsx` — LT Trend, ST Trend, BTFD/STFR (open + closed) all removed
- Intrabasket now shows only: **Correlation**, **Returns**, **Contribution**
- Removed `OpenSignal` interface, `SignalsTable` component, `SIGNAL_FILTERS`, `LIVE_ROW_COLORS`, `pctFmtCell`, `dollarFmtCell`, `colorForPerfCell` dead code
- Removed `OpenSignal` and `open_signals` from `App.tsx` `BasketSummaryData` interface
- Default tab changed from `'breakout'` to `'correlation'`
- Clean TypeScript build confirmed (`npx tsc --noEmit`)

### Files Modified
- `signals/live_updates.py` — rewrote `export_today_signals()`
- `signals/loop_universes.py` — **NEW**
- `signals/loop_signals.py` — **NEW**
- `signals/loop_live.py` — **NEW**
- `ecosystem.config.js` — replaced live-signals with 3 new processes + docs
- `app/backend/main.py` — schema filtering, THEMATIC_CONFIG keys, correlation breadth, industry filter
- `app/frontend/src/components/BasketSummary.tsx` — removed signal tabs and dead code
- `app/frontend/src/App.tsx` — removed OpenSignal interface
